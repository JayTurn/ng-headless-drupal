/**
 * Base Main controller tests.
 */
'use strict'

describe('Main Controller', function () {
  console.log('Inside base main test');
  it('should return a test', function () {
    var a = 1;

    expect(a).toEqual(1);
  });
});
